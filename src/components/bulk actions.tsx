import React from 'react';
import '../styles/bulk.scss'; 

const Buttons: React.FC = () => {
    return (
      <div className="buttons-container">
        <button className="bulk-actions-button">Bulk Actions</button>
      </div>
    );
  };

export default Buttons;