import React from 'react';
import '../styles/header.scss';

const Header: React.FC = () => {
    return (
      <header className="header">
        <h1 className="header-title">Contracts</h1>
      </header>
    );
  };

export default Header;